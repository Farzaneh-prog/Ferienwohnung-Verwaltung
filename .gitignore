# Internal planning docs — contain real cleaner names, pay rates, and
# contract dates. Never publish these; kept local-only on purpose.
سند-معماری-سیستم-مهمانخانه.md
نظافتچی_ها-و-فرمول_های-مالی.md

# Secrets
.env

# Real reservation data (never commit real guest data)
GästeListe_*.xlsx
reservations.xlsx
!sample_data/*.xlsx
backups/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Excel lock files
~$*.xlsx
wordpress-plugin/marktresidenz-secrets.php

# Real platform account/listing identifiers — operational business info,
# not published alongside the generic template in docs/cowork-command.md
docs/cowork-command.local.md
